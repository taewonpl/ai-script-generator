# 경량화된 Shared Core 의존성 for AI Script Generator v3.0

# 필수 의존성만 포함 (SQLAlchemy, uvicorn 제거)

# 데이터 검증 및 직렬화
pydantic>=2.5.0

# 환경 변수 관리
python-dotenv>=1.0.0

# FastAPI (스키마 검증용)
fastapi>=0.104.1

# Settings management
pydantic-settings>=2.1.0

# HTTP requests
requests>=2.31.0
aiohttp>=3.9.0

# Type stubs for MyPy
types-redis>=4.6.0
types-requests>=2.31.0
types-aiohttp>=3.9.0
